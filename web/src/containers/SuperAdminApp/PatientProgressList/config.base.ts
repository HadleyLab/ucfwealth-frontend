import { AccountId, PrivateKey } from '@hashgraph/sdk';

export const MY_ACCOUNT_ID = AccountId.fromString('id');
export const MY_PRIVATE_KEY = PrivateKey.fromString('private key');
export const TREASURY_ID = AccountId.fromString('id');
export const TREASURY_KEY = PrivateKey.fromString('private key');

export const CID_ARRAY = ['cid'];

// Create config.ts and enter your data